# Business deps are added in the phases that first use them (bcrypt → 7).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
psycopg[binary]==3.2.3

# Phase 2 — Plaid Link flow + encrypted access_token at rest.
plaid-python==40.1.0
cryptography==49.0.0

# Phase 4 — verify Plaid webhook JWTs (ES256). Reused for HS256 session JWTs (7).
pyjwt==2.13.0

# Phase 7 — hand-rolled auth: bcrypt password hashing (login + set-password CLI).
bcrypt==4.2.1
